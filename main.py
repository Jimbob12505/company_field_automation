# Entry point

# method called read_file(filename). Reads the csv file and returns an list[i][j]
# where list[i] is a row i in the csv file

# method called parse_data(list). Takes in a list of lists returns a list containing
# either the company_domain or the company name


# method called open_ai_request(). Sends an openAI API call